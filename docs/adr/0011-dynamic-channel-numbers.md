# 频道号改为排序后位置动态生成

原定义"频道号 = 全局 orderIndex + 1"（与分组无关），删除频道后留下空洞（如删了 3 号，4、5 号不变，出现跳号）。用户要求"每次完整重排"。改为：**频道号 = 按 orderIndex 排序后的列表位置 + 1，查询时动态生成**（/api/channels 与电视端 Channel 模型一致），永远连续无空洞。选动态生成而非写库压实 orderIndex：无写库副作用、天然连续、实现简单；代价是频道号随排序变化（用户已接受"每次完整重排"语义）。
