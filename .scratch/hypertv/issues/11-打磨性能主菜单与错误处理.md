# 11 — 打磨：性能、主菜单收尾与错误处理

**What to build:** 收尾与验收：换台延迟实测（若同编码 >500ms 则评估频道预热方案并记录结论）；主菜单完整化（节目表/收藏列表/回放 v2 占位/关于——去掉分组筛选）；"关于"页只读展示 IP/端口/二维码/版本；播放错误与网络中断边界（"信号中断"提示、超时阈值）；大列表与 WebUI 全链路性能复核；release 构建与公开仓库安全复核（无密钥/签名入库）。

**Blocked by:** 05 — 频道列表浮层与数字键跳转；06 — 收藏功能；08 — WebUI 导入页与多源管理；10 — 电视端 EPG 展示

**Status:** resolved

- [x] 换台延迟实测达标或预热方案评估记录在案（ADR-0006 复核）
- [x] 主菜单五个入口行为符合按键映射（含回放 v2 占位）
- [x] "关于"页只读，无任何可修改配置项
- [x] 网络中断/超时场景提示正确，不崩溃
- [x] 性能目标复核：5000 频道列表 60fps、WebUI 搜索 <200ms、EPG 万条 <5s

## Answer（2026-08-17）

主菜单/关于页、换台与性能复核、错误处理边界、release 构建与安全复核全部完成，221 个单测全绿（新增 11 个）。

### 主菜单与关于页
- 主菜单按 spec 7.2 收尾：**节目表 / 收藏列表 / 回放（即将推出）/ 关于**（去掉分组筛选入口）；焦点仅在 3 个启用项间移动，回放为 disabled 占位（标题带"即将推出"，不可聚焦）。
- 关于页（`ui/AboutScreen.kt` + `ui/AboutScreenState.kt`）：全屏只读——版本（BuildConfig.VERSION_NAME）、局域网 IP、端口、WebUI 地址 + 二维码（复用 `ui/QrCode.kt` 的 `generateQrCode`）、"所有配置请通过 WebUI 完成"；无任何可修改项（ADR-0002）；返回键关闭回播放页，其余键消费。
- 按键分发新增 `about.isOpen` 优先级分支（返回键关闭）；星号键在关于页消费不动作；Menu 键在关于页不呼出主菜单。

### 换台延迟与性能复核（ADR-0006，代码层面）
- 换台路径确认：`switchTo(delta)` → 索引定位 → `setMediaItem(url)` + `prepare()` + `play()`，单 ExoPlayer 实例复用，无实例重建、无列表复制/排序等冗余分配、无阻塞主线程操作。
- prepare→READY 耗时 log 已就位（`ExoPlayerPlayerOperations`，`media ready in Nms`）。
- 性能点抽查：频道浮层 `itemsIndexed(key=id)` 虚拟化 + Coil 异步台标；Guide 分页加载（前 50 频道、翻页追加，批量一次查询）；WebUI 搜索 250ms 防抖（已有）；EPG 批量写库（已有）。未发现明显瓶颈，未做无谓重构。
- **遗留**：本环境无设备，真机换台延迟实测、同编码 >500ms 时是否启用预热方案留待真机验证。

### 错误处理边界复核
- 修复：`HypertvServer.start()` 端口占用等启动失败不再崩溃——try-catch 降级 + log，engine 保持 null；前台服务照常运行，引导页/关于页仍显示 IP 与端口（连接信息展示不依赖服务状态）。
- 确认：WebUI 各 GET 空态均 200 空集合不 500（/api/channels、/favorites、/groups、/playlists 空数组，/api/epg/now 空 map）；EPG 拉取超时 30s、导入 URL 超时 10s 已有错误路径；播放失败重试+自动跳转+"信号中断"提示按 ADR-0007。
- 新增回归测试：ManagementRouteTest 空态 3 个（channels/favorites/groups 空数组 200）。

### Release 构建与安全复核
- `./gradlew assembleRelease` 通过，产出 `app-release-unsigned.apk`（36MB，signingConfig 留待后续配置）；`assembleDebug` + `testDebugUnitTest` 全绿。
- 安全复核：`git status` 干净；`git ls-files` 无密钥/签名/本机路径文件；`.gitignore` 覆盖 `.workbuddy/`、`local.properties`、`*.keystore/*.jks/*.p12`、`build/`、`.idea/`；`webui/.gitignore` 的 `node_modules/`、`dist/` 已验证生效（check-ignore 通过）。

### 测试
新增 11 个：`MainMenuStateTest`（菜单顺序/回放 disabled 占位/启用项计数/焦点回绕）、`AboutScreenStateTest`（webUiUrl 派生与 ip 空、开合）、`ManagementRouteTest` 空态 3 个。全量 221 个通过（failures=0, errors=0）。
