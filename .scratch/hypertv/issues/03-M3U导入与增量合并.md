# 03 — M3U 导入与增量合并

**What to build:** 从 URL 拉取或文件上传解析 M3U/M3U8（含 GBK/UTF-8 编码识别），输出规范化频道列表（名称/URL/分组/台标/tvg-id/catchup 字段）并在导入前生成解析预览。导入执行按频道 URL 增量合并：已存在频道保留收藏与自定义顺序、更新元数据，新增频道追加，源内消失频道标记隐藏。API：POST /api/playlist/import、POST /api/playlist/upload。

**Blocked by:** 02 — 数据层：Room schema 与 Repository

**Status:** resolved

- [x] URL 导入 5000 频道解析 <5s
- [x] UTF-8 与 GBK 编码的 M3U 均可正确解析
- [x] 导入前返回预览（频道数、分组列表）
- [x] 重复导入同一源：收藏与自定义顺序保留，消失频道标记隐藏而非删除
- [x] 解析器单测覆盖异常行、缺失字段、空源

## Answer（2026-08-17）

Commit `8fa9f6e` 实现并验收通过：EncodingDetector（BOM 优先 + UTF-8 严格试探回退 GBK，零第三方依赖）、M3uParser（EXTINF 属性解析、#EXTGRP 优先级、容错）、IncrementalMerger（URL 归一化匹配、保留收藏/顺序/ID、消失频道标隐藏、新增续排）；API：POST /api/playlist/import/preview（预览不落库）、/api/playlist/import（事务落库）、/api/playlist/upload（multipart）；ChannelDao.persistImport @Transaction 原子写。构建成功，53 个单测全过（新增 34 个）。遗留：文件上传每次视为新源（refresh 按 id 刷新留待后续）；5000 频道解析性能未基准。
