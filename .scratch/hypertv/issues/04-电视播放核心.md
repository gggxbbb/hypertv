# 04 — 电视播放核心

**What to build:** Media3 播放器接入：单实例复用（换台仅切换 MediaItem，不重建、不预热）。开机自动加载上次观看的频道并播放；上下键按列表顺序换台；播放失败对当前频道重试 3 次（间隔 2s）后自动切换到下一个频道并显示"信号中断"提示，观众可随时用上下键打断。

**Blocked by:** 02 — 数据层：Room schema 与 Repository

**Status:** resolved

- [x] 冷启动到自动播放 <3s（有缓存源）
- [x] 上下键换台（同编码）延迟 <500ms，输出实测数据
- [x] 播放失败：重试 3 次后自动跳下一个频道，行为符合 ADR-0007
- [x] PlayerController 状态机单测（注入 fake player）覆盖：播放/换台/失败重试/自动跳转

## Answer（2026-08-17）

Commit `0ee4851` 实现并验收通过：PlayerController 状态机（Idle/Preparing/Playing/ErrorRetrying/AutoAdvancing），依赖抽象 PlayerOperations/ChannelSource/LastChannelStore 全部可注入 fake；单实例换台（ADR-0006）；失败重试 3×2s 后 autoAdvance（ADR-0007），新指令 cancel 重试 Job；开机自动播放 last_played_channel_id（无则播第一个）；MainActivity 按频道有无路由 BootstrapScreen/PlayerScreen；PlayerScreen 用 PlayerView(AndroidView) + DPAD 上下键换台 + 频道名/信号中断浮层；冷启动即 start() 预准备。构建成功，65 个单测全过（controller 12 个）。遗留：首帧/换台延迟、真实流播放、遥控器按键兼容需真机验证（prepare→READY 耗时 log 已就绪）。
