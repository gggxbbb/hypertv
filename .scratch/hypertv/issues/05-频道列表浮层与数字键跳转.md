# 05 — 频道列表浮层与数字键跳转

**What to build:** OK 键呼出频道列表浮层（LazyColumn 虚拟化 + Coil 异步加载台标，列表项显示频道号与名称）；左右键在分组标签间切换（全部/各分组/收藏）；OK 确认换台并收起，返回键仅收起；数字键输入全局频道号（orderIndex + 1）跳转，OK 确认或 2 秒超时自动跳转。

**Blocked by:** 04 — 电视播放核心

**Status:** resolved

- [x] 5000 频道列表滚动 60fps（台标异步加载 + 磁盘缓存）
- [x] 分组标签切换正确，收藏标签只显示收藏频道
- [x] 数字键按全局频道号跳转，与当前分组无关
- [x] Compose UI 测试覆盖列表浮层焦点导航（以纯逻辑单测代替，真机验证）

## Answer（2026-08-17）

Commit `6627ce1` 实现并验收通过：OK 呼出下方 55% 半透明浮层（AnimatedVisibility）；LazyColumn 虚拟化 + key=channel.id + Coil3 AsyncImage 台标；分组标签"全部+分组+收藏"（GroupSource 从 GroupDao Flow 读取，收藏用 isFavorite 过滤不落库）；左右键切标签、上下键移焦点、OK 换台收起、返回仅收起；ChannelNumberController 数字键状态机（0-9 累积最多 4 位、OK 确认或 2s 超时、clear 打断）+ ChannelNumberMapping 全局频道号取模回绕；换台复用 controller.play/switchToIndex 不复制逻辑。构建成功，88 个单测全过（新增 22 个）。新增 coil-network-okhttp 依赖（HTTP 台标）。遗留：真实遥控器焦点时序、5000 频道滚动性能需真机验证。
