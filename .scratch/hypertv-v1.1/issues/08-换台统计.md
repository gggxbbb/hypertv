# 08 — 换台统计

**What to build:** 换台性能的长期数据：每次换台记录"流就绪耗时（STATE_READY）"与"首帧近似耗时"（videoSize 首次变化），Room 持久化，WebUI 面板展示聚合指标（均值/p95/最近 20 条明细）——让换台 <500ms 目标有真实数据可追踪。电视端零 UI。

**Blocked by:** 01 — 数据层 schema v6 + 字段扩展

**Status:** ready-for-agent

- [ ] PlayerController 换台路径打点：确认换台 → `STATE_READY` 耗时；首帧近似 = videoSize 首次变化（监听器）
- [ ] 统计写入 `channel_switch_stats` 表（channelId/时间戳/readyMs/firstFrameMs）
- [ ] 聚合查询：均值 / p95 / 最近 20 条明细
- [ ] API：`GET /api/stats/channel-switch`（聚合 + 明细）
- [ ] WebUI 独立"换台统计"面板（电视端零 UI）
- [ ] 单测：打点时序（fake player 控制 ready/firstFrame）、聚合计算；Ktor 路由契约测试
