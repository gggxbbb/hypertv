# 06 — 回放能力探测

**What to build:** 判断哪些频道"真的"支持时移回放：复用探测引擎对声明过 catchup 的频道额外发一个回放窗口请求，把结论（catchupSupported）落库——时移入口只对确认支持的频道开放，避免用户对无效回放操作产生困惑。

**Blocked by:** 02 — 探测执行器（ProbeRunner）

**Status:** ready-for-agent

- [ ] 对声明过 catchup（append/default）的频道，探测时额外发一个回放窗口请求（playseek 过去时间段）验证回放能力
- [ ] 回放请求返回有效 m3u8 列表 → `catchupSupported=true`；失败/异常 → false
- [ ] 结果落 channels 表 `catchupSupported`，随探测结果一起刷新
- [ ] API 暴露（/api/channels DTO 已含该字段，验证链路通）
- [ ] 单测：声明判定、回放请求成功/失败分支、随探测刷新语义
